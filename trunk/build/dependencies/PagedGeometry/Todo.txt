__PagedGeometry________________________
+	Generalize the entire PagedGeometry structure so it can be easily expanded
+	Re-write the static geometry and imposter code as extensions of GeometryPage
+	Find a new way to prevent pageload clashes (where more than one manager loads a page during the same frame), since this can cause slight stuttering.
+	Add reloadGeometry()
+	Remove PagedGeometry bounds
+	Add a quality GrassLoader implementation
+	Add a quality TreeLoader implementation
+	Smoothly fade between detail levels
	+	Overlap LODs
	+	Use GeometryPage::setFade() to fade in LODs smoothly
	+	Implement GeometryPage::setFade() for all page types
		+	Rewrite StaticGeometry
		+	Write fade shader manager
			+	BatchPage
			+	ImpostorPage
		+	No shader fallback
+	Make grass system automatically disable shadows and lighting on the given material
+	Add GrassLoader::setRenderQueueGroup()
+	Update documentation for GeometryPage
+	Implement GrassLayer::setFadeTechnique()
+	Review all uses of Ogre::Except
+	Remove not-yet-implemented functions
+	Change i++ to ++i
+	Remove using namespace Ogre in headers
+	Review STL data types for speed
+	Add 3D TreeLoader version
+	Optimize includes
+	Remove timer.h
+	Fix 1.2.x issues
+	Seed grass system
+	Don't render shadows in impostor system
+	Further optimize headers
+	Fix fade bug with non alpha-rejected materials
+	Add ImpostorPage::setBackgroundColor()
+	Add PagedGeometry::setCamera()
+	Eliminate the need to #define the height function
+	Fix frame listener destruction
+	Fix impostor alpha sorting issue
+	Fix Impostor image selection issues
+	Add setPageSize() and setBounds()
+	Fix problem with <=0 fade ranges
+	Fix more 1.2.x issues
+	Allow impostor slices to be adjusted
X	Implement EntityPage fade
X	Alternate height function callback method
+	Fix fade bug with multiple impostored entity types
+	Fix fade loose grid tile bounds
	Fix BatchPage shader inconsistancy with fixed-function
	Remove EntityPage
	
	Optimize grass loadPage()
	Write tutorials and examples
	
--- Release Version 1.0 ---
	
	Add GrassLoader::sprayGrass()
	Add density map tree loader
	Add hard disk tree loader
	Add BillboardPage
	Improve impostor customization
		setDefaultImpostorResolution(), setDefaultImpostorColor(), setDefaultImpostorRenders()
		setImpostorResolution(), setImpostorColor(), setImpostorRenders()
	Implement InstancePage

	Add a maximum view height as well as a maximum view distance for detail levels
	Optimize impostor generator for tall, skinny trees
	Fix Ogre's billboard system for camera roll
	Experement with single-triangle billboards and point-sprites
	Add collision interface
	General optimizations (add const's and prefix operators, etc.)
	Possibly add a pseudo-volumetric-texture GeometryPage implementation

__BatchPage________________________
+	Add batched geometry system

__ImposterPage________________________
+	Add basic impostering system
	+	Imposter renderer
	+	Mip-mapped imposters
	+	8-direction imposter textures
	+	Use saved .png files instead of rendering whenever possible
	+	Correct imposter rotation
+	Add top-view of imposters
+	Find a way to remove the need for 8 materials for each imposter type
+	Fix impostor image selection algorithm
+	Make sure billboards are displayed properly when camera is upside down
+	Improve impostoring performance through custom billboards
+	Instead of 2 "pitch" billboard renders (top/front), use 4
+	New billboard system should fall back to Ogre's billboard system on older hardware
+	Fix StaticBillboardSet visibility bug where build() and clear() would reset visibility status
	Improve billboard system loading method (if possible)

__EntityPage________________________
+	Add simple entity system
+	Speed up entity system by creating a "pool" of entities
	Speed up pooling system by setting entity visibility rather than attaching/detaching scene nodes
	
__Example Application________________________
+	Modify the example application to use the example framework, so it will work with both Dagon and Eihort

__Documentation________________________
+	Make all the code comments compatible with doxygen
+	Write documentation
